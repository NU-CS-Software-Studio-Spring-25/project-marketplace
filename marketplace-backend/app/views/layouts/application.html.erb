<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "course-match" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="<%= content_for(:meta_description) || 'Course Match - Find your perfect classes. Browse, filter, and discover courses that match your interests.' %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag 'favicon.svg' %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">



    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>


    <link rel="apple-touch-icon" href="/icon.png">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Varela+Round&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Custom CSS -->
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "hinge_theme", "data-turbo-track": "reload" %>
    
    <%= javascript_importmap_tags %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </head>

  <body>
    <header role ="banner">
        <%= render 'shared/navbar' %>
    </header>
    <% if flash[:alert] %>
      <div class="container mt-3" role = 'alert'>
        <div class="alert alert-danger">
          <%= flash[:alert] %>
        </div>
      </div>
    <% end %>
    
    <main role="main" >
      <%= yield %>
    </main>

    <% unless current_page?(swipes_path) %>
      <footer class="footer-hinge" role ="contentinfo">
        <%= render 'shared/footer' %>
      </footer>
    <% end %>
  </body>
</html>
